/*The program below computes the circumference of a circle,
 *it first takes the diameter that the user inputs and,
 *divides it by 2 to get the radius and then, it inputs that,
 *radius into the orignial formula to find c
 *it can compute the results for floating and integer types
 */ 

#include <stdio.h>
#define PIE 3.14

int main()
{
    float d;
    float prod;
    float c;
    printf("Circumference of a circle (C) = 2*pi*r\n");
    printf("C= circumference, d= diameter, r= radius\n");
    printf("Enter the diameter\n");
    scanf("%f", &d);
    prod = d / 2;
    printf("r = %f", prod);
    c = 2 * PIE * prod;
    printf("C = %f", c);
    
    

    return 0;
}
