#given a list
listed = [4,5,6,7,8,1,2,3]
#empty userlist to add values to it
userlist = []
#for loop to take input from user restricted to 4.
for i in range (1,5):
    user_1 = int(input('Enter Number: '))
    userlist.append(user_1)
#combined both lists    
temp = userlist + listed
#sorted both lists in temp
temp.sort()
print (temp)

    
